// então, já vimos:
//    - HTML, CSS, JS
//    - IF
//    - WHILE, FOR
//    - VAR
//    - ARRAY, VETOR
        
// agora prtiu ver FUNÇÕES:

// onclick = quando clicarmos em tal coisa acontece outra x coisa; ou o evento pode ser linkado no html ou guardado numa variável convocado através de um querySelector

// A nomeação da função é como na variável + ( * ) e {}

    // * dentro dos parenteses colocamos os parâmetros ou argumentos quando necessário

// .endsWith() <- espera uma string;

// .innerHTML = <- 

// inputAddFavoriteMovie.value = ""; <- limpamos o campo do input atribuindo a ele o valor null

// document.querySelector() <- ?
